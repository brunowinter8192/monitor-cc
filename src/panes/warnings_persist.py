# INFRASTRUCTURE
# (append_tool_errors removed in Stage 2D — errors written to _errors dual-log by proxy write-side)

# FUNCTIONS

# Stub retained for import compatibility; no longer called.
def append_tool_errors(new_errors: list, project_filter: str = '') -> None:
    return
