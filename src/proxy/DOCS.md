# src/proxy/

## Role

mitmproxy addon package. Loaded at proxy startup via `src/proxy_addon.py` (thin `-s` entry point).
Intercepts every POST `/v1/messages` request from Claude Code, applies a deterministic modification
pipeline (rule injection, content stripping, MCP tool injection, fixation, cache marker placement),
logs the result, and forwards the modified payload to Anthropic. Touch this package when changing
what gets injected, stripped, or cached. Do NOT edit files here during a live proxy session —
the running proxy uses a frozen copy in `src/logs/.proxy_live_<id>/proxy/`. See Gotchas.

## Public Interface

`__init__.py` is a package marker only — no exports. External callers use `src/proxy_addon.py`
which mitmproxy loads via `-s` flag.

## Flow

mitmproxy `http.HTTPFlow` (POST /v1/messages) → `addon.ProxyAddon.request()`
→ `rules` (system2 + project rule injection, content strip)
→ `fixation` (freeze sys[2] + msg[0] after first request) → `tools` (blocklist strip)
→ `tool_injection` (MCP schema append) → `inject_helpers` (model override, context management)
→ dual-log writes (original + forwarded + errors) → `cache` (strip all markers, set BP3/BP4/anchor)
→ modified payload forwarded to Anthropic; `response()` hook writes stripped/injected dual-logs via metadata bridge

## Modules

### addon.py (421 LOC)

**Purpose:** Core mitmproxy addon class — receives HTTP flows, orchestrates the full modification pipeline, writes dual-log entries, appends 4xx errors to `api_errors.jsonl`. count_tokens requests (`/v1/messages/count_tokens`) pass through unmodified — `_is_messages_request()` matches only `/v1/messages` + optional query string. **(2026-09, addon-split milestone) `ProxyAddon` was carrying 15 `self.<attr>` — split into 4 collaborator objects, one per concern, all defined in `addon_state.py` (see that module's entry):** `self.paths` (`DualLogPaths` — the six dual-log file paths), `self.delta` (`DeltaState` — the five per-model-family delta-chain dicts), `self.fixation` (`FixationState` — sys2/msg0 fixation + model-params fixation), `self.identity` (`SessionIdentity` — `session_id`/`worker_context`). `ProxyAddon.__init__` now holds exactly these 4 attributes. `request()` (was 74 LOC) and `response()` (was 51 LOC) both dropped under 50 by extracting `_apply_sys_fixation`, `_stamp_request_metadata`, `_finalize_cache_state`, `_write_request_dual_logs` (request-side) and `_log_4xx_error`, `_write_stripped_injected` (response-side) — see Key functions. Byte-identical — verified via `dev/proxy/pipeline_byte_identity.py` (pure-function pipeline) and the new `dev/proxy/addon_hook_byte_identity.py` (the hook methods themselves, which nothing else in `dev/` drives).
**Reads:** mitmproxy `http.HTTPFlow`; env vars `MONITOR_CC_ROOT`, `PROXY_LOG_ID`, `PROXY_PROJECT_PATH` for log path resolution and session/worker context.
**Writes:** Modifies `flow.request.content` in place; appends one JSONL line to `src/logs/api_errors.jsonl` on 4xx. Writes six dual-log files via `_resolve_dual_log_file(suffix)` into `src/logs/dual_log/`: `_original` (raw CC payload before `apply_modification_rules`); `_forwarded` (delta entry via `_build_forwarded_delta` — REQ#1 full, subsequent only changed elements per hash diff; always carries `max_tokens` + `output_config` scalars + `anthropic_beta` list of CC beta-feature flags from the HTTP request header); `_stripped` and `_injected` (delta entries via `_build_stripped_injected_deltas` — written in `response()` hook via metadata bridge); `_errors` (`is_error=True` tool_result blocks from original payload, dedup by `tool_use_id` per model_family, written in `request()` hook); `_response` (Anthropic response HTTP headers filtered via `_filter_response_headers` — rate-limit family + `request-id` + `retry-after` — written in `responseheaders()` hook for ALL status codes). Each write in its own `try/except`; failures never affect forwarding. **`request()` also calls `_trigger_bg_escape(stripped_msg_removed, self.identity.worker_context, project_path)` (2026-07-30, `bg_escape.py`)** — own `try/except` wrapper, same defensive convention as every other per-concern block in this hook; fires a tmux Escape into the worker's pane at most once per background-task id (see `bg_escape.py`). `apply_modification_rules(...)` is now called with `self.identity.worker_context` as a 4th positional arg (2026-08-06) — selects the bg-launch-ack replacement wording, see `strip_bg_launch_ack.py`. (Milestone 3, 2026-08: the sibling `_update_pending_bg_state` call this paragraph used to describe — `pending_bg_state.py` — was removed; `bg_escape` is the only per-concern side-effect call left in `request()`, see `process-docs/timer-loop/` area.) **Model-params fixation (2026-09):** `_run_post_fixation_pipeline` now takes a 5th arg, `self.fixation.model_params_fixated`, threaded straight into `_inject_model_override` (`inject_helpers.py`) — pins the resolved `model_params`/legacy-override snapshot per exact model id for the process lifetime, the same rationale and mechanics as `self.fixation.fixated`'s sys2/msg0 protection (see State, and `process-docs/cache/cache_rebuild_cases.md` Case 3) but a SEPARATE dict, since its key granularity (exact model id) and its owning function (`inject_helpers.py`, not `fixation.py`) both differ from `self.fixation.fixated`'s.
**State:** `self.delta.messages_by_model` (dict, keyed by model_family) — message summaries from previous request; used by `_set_cache_breakpoints` for BP3 unchanged-prefix detection. `self.delta.forwarded_hashes_by_model` — per-element hash lists for `_forwarded` delta chain. `self.delta.stripped_hashes_by_model` / `self.delta.injected_hashes_by_model` — flat `loc_key → MD5[:10]` dicts for `_stripped`/`_injected` delta chains. `self.delta.error_ids_by_model` — set of `tool_use_id` strings already written to `_errors`; dedup guard. `self.fixation.model_params_fixated` (2026-09, dict keyed by EXACT model id, not model_family) — the resolved `model_params` entry or legacy-override section snapshotted on the first request seen for that model id; every later request for the same model id replays the snapshot instead of re-reading `proxy_rules.json`, so a mid-session config edit cannot change an already-running proxy process's injected `thinking`/`effort`/`max_tokens`/`model`. Owned entirely by `inject_helpers._inject_model_override` — `addon.py` only holds and threads the dict through, never reads/writes it directly. `self.identity.session_id` (computed once via `_derive_session_id`) — `md5(PROXY_PROJECT_PATH)[:8]`. `self.identity.worker_context` (computed once via `_derive_worker_context`) — `"main"` or `"worker:<name>"`, also the input `bg_escape._trigger_bg_escape` uses to gate worker-vs-main and derive the tmux session name. A few dev/ probes reach in and overwrite `addon.identity.worker_context` directly after construction (`dev/bg_wakeup_id_line/p2_bg_escape_probe.py`, `dev/native-model-start/p3_cache_breakpoints_probe.py`, `dev/timer-loop/p3_project_scope_incident_probe.py`) — same pattern as before the split, just re-pointed onto the collaborator. Metadata bridge: `mc_original_payload`, `mc_modified_payload`, `mc_model_family`, `mc_all_ops`, `mc_request_id` — stored on `flow.metadata` in `request()`, read in `response()`. (`mc_stripped_msg_removed` / `mc_injected_msg_added` still stashed in `request()` but no longer consumed in `response()` — dead stashes.)
**Called by:** mitmproxy (via `addons = [ProxyAddon()]` at module level). Hooks: `request`, `responseheaders`, `response`.
**Calls out:** `mitmproxy`, `bg_escape`, `addon_state`
**Key functions (FUNCTIONS section):** `_filter_response_headers(headers) → dict` — filters Anthropic response headers by exact name (`request-id`, `retry-after`, `anthropic-organization-id`) or prefix (`anthropic-ratelimit-*`, `anthropic-priority-*`, `anthropic-fast-*`); normalizes keys to lowercase. Constants: `_RESPONSE_HEADER_EXACT` (frozenset), `_RESPONSE_HEADER_PREFIXES` (tuple). `request()` decomposed (C2, 2026-09 extended): `_log_errors_entries`, `_log_forwarded_delta`, `_run_post_fixation_pipeline`, `_log_original_request`, `_infer_model_family`, plus (addon-split milestone) `_apply_sys_fixation` (capture/replay fixation.fixated), `_stamp_request_metadata` (request-id/timestamp + metadata-bridge stash), `_finalize_cache_state` (cache-control strip + breakpoints + prev-message-summary update), `_write_request_dual_logs` (forwarded-delta + errors writes, folded into one call). `response()` decomposed into `_log_4xx_error` (the 4xx branch body) and `_write_stripped_injected` (the success-branch stripped/injected write, still wrapped in `response()`'s own try/except).

---

### addon_state.py (35 LOC, new 2026-09, addon-split milestone)

**Purpose:** Four plain-class collaborators holding `ProxyAddon`'s per-concern instance state — `DualLogPaths` (the six dual-log file `Path`s), `DeltaState` (the five per-model-family delta-chain dicts used for BP3 detection and the `_forwarded`/`_stripped`/`_injected`/`_errors` dedup chains), `FixationState` (`fixated` — sys2/msg0 snapshot per model_family, `model_params_fixated` — per-exact-model-id snapshot, see `inject_helpers.py`), `SessionIdentity` (`session_id`, `worker_context`, both computed once at `ProxyAddon.__init__` and immutable for the process lifetime in production — a few dev/ probes overwrite `worker_context` directly after construction, see `addon.py`'s State entry). No class holds 10 or more attributes (6/5/2/2). Utility module — no ORCHESTRATOR/FUNCTIONS split, plain `__init__`-based classes only.
**Reads:** Nothing — pure state containers.
**Writes:** Nothing at module level — instances are mutated by `addon.py`'s hook methods and helpers.
**Called by:** `src/proxy/addon.py` (`ProxyAddon.__init__` constructs one of each; every hook method and FUNCTIONS-section helper threads them through by parameter).
**Calls out:** —

---

### bg_escape.py (134 LOC)

**Purpose:** Forces a worker idle by sending `tmux send-keys ... Escape` into its pane the instant the proxy detects a genuine CC background-launch ack (`strip_bg_launch_ack.py`'s `_is_bg_launch_ack`) — so it has no turn left in which to poll the newly-backgrounded task. Fires at most once per task id, ever, per proxy process (2026-07-30) — the launch-ack's raw text stays in CC's own conversation history and is resent + re-detected on nearly every subsequent request (measured 142/169), so a naive fire-on-detection would re-Escape on almost every request; a second Escape into an idle CC TUI opens the quit menu, which is the hard reason this must be exactly-once, not just wasteful. Reuses `strip_bg_launch_ack.py`'s detection (`_is_bg_launch_ack`) and id-extraction (`_ACK_ID_RE`) unchanged — does NOT alter that module's detection/replacement semantics. **Logs every fire and every meaningful skip (2026-07-30):** the earlier `has_bg`-based mechanism this replaced was diagnosed via its own log (`menubar.log`), and that log is exactly what exposed its failure — `bg_escape.py` had none until this addition. `_log_bg_escape_event` writes one JSONL line for a "fired" event (task id, tmux session, `send_result`) and for a "skipped" event with a `reason` (`main_context`, `already_escaped`, `no_task_id`, `no_tmux_session`) — but ONLY when `_is_bg_launch_ack` actually matched a chunk this request; a request carrying no ack at all never touches the log sink, keeping the per-request hot path log-free.
**Reads:** `stripped_msg_removed` dict passed by `addon.py` (per-request removed chunks from all message passes — scanned for one whose text is a genuine ack); `worker_context` / `project_path` strings passed by `addon.py` (`self._worker_context`, `PROXY_PROJECT_PATH`); `MONITOR_CC_ROOT` env var (log path resolution).
**Writes:** `_escaped_task_ids` (module-global `set`, in-memory only — see State). Side effect: one `tmux send-keys -t <session> Escape` subprocess call per newly-seen task id. Appends one JSONL line per fire/meaningful-skip to `bg_escape_events.jsonl` — `MONITOR_CC_ROOT/src/logs/bg_escape_events.jsonl` if set, else `/tmp/bg_escape_events.jsonl` — same flat-top-level-file convention as `addon.py`'s `api_errors.jsonl`, NOT stderr (mitmdump runs production with stderr routed to `/dev/null`, `src/claude_proxy_start.sh` — a stderr-only trace would be invisible in production).
**Called by:** `src/proxy/addon.py` (`ProxyAddon.request()`, own `try/except` wrapper).
**Calls out:** `subprocess` (`tmux has-session` / `tmux send-keys`, stdlib only otherwise).

---

### rules_config.py (107 LOC)

**Purpose:** Load and cache proxy rule files — reads `proxy_rules.json`, caches rule file content by mtime, assembles system2 rule text for a given session ROLE and project path. **`_load_system2_rules(model_family, project_path="", worker_context="")` is role-keyed (2026-08-29):** `role_key = "worker" if (worker_context or "").startswith("worker:") else "main"` picks the `system2_rules.worker` or `system2_rules.main` file list, concatenated as `global + role + project`. The role comes from `addon._derive_worker_context` (`PROXY_LOG_ID`), NOT from the model family — model and role are independent since the menubar Models tab, so an opus-family worker gets worker rules. Absent/empty/`None` `worker_context` (dev probes and replay tools calling with 3 args, one passing `None`) resolves to `main`. `model_family` has exactly one job left here: the haiku short-circuit, which returns `""` before any role handling — haiku sidecar requests occur inside main sessions and must not receive main rules. The former `"opus"` config key was renamed to `"main"` with no fallback (one-shot migration, config edited out-of-repo). **(2026-09, helper-extraction milestone)** the project-specific-file resolution loop (incl. the exclusive-project/model-family early-empty-return) split into `_resolve_project_rule_files(s2, project_path, model_family) -> (project_files, exclusive_files, empty_due_to_family)` — `_load_system2_rules` itself now checks `empty_due_to_family` and returns early, same behavior.
**Reads:** `~/.claude/shared-rules/proxy_rules.json` and rule files (mtime-cached).
**Writes:** Nothing — returns config dict or assembled rule text.
**Called by:** `rules.py`, `message_passes.py`, `inject_helpers.py`
**Calls out:** stdlib only (`json`, `pathlib`).
New private helpers (same module): `_resolve_project_rule_files` (2026-09).

---

### rules.py (163 LOC)

**Purpose:** Orchestrates the proxy modification pipeline. Exports `apply_modification_rules(payload, model_family="opus", project_path="", worker_context="")` — loads system2 rules, runs the message passes (4 from `message_passes`, 8 from `message_passes_simple` — see those modules' own entries for the split) via a `_passes` loop (each pass updates `new_messages` in sequence, accumulates `modifications`/`stripped_msg_indices`/`stripped_msg_removed`/`injected_msg_added`/`_all_ops`), then calls `_dedup_wakeup_blocks` (`message_passes_wakeup.py`) and `_apply_system_passes`. Re-exports `_strip_blocked_tool_references` (imported from `payload_helpers`) for `addon.py`. Contains `_apply_system_passes`: replaces `system[1]` with `"."` when its text exact-matches `_SYS1_BOILERPLATE_TEXT` (CC's constant `"You are Claude Code, Anthropic's official CLI for Claude."` line, redundant once `system[2]` is replaced with our own rules — mod `stripped_sys1_boilerplate`; exact-match guarded, not index-alone, so an unrecognized future CC value at index 1 passes through untouched), injects system2 rules into `system[2]`, strips session-guidance and gitStatus from `system[3]`, normalizes worktree paths in `system[3]`. All three are content-replacement, never block removal — `system` array length (and `strip_inject_delta.py`'s logged `counts.system`) never changes. **`worker_context` param (2026-08-06, milestone-2):** `is_main = worker_context == "main"`; a closure `_bg_launch_ack_pass = lambda msgs: _apply_bg_launch_ack_strip(msgs, is_main=is_main)` is swapped into the `_passes` list at the SAME position `_apply_bg_launch_ack_strip` always occupied (pipeline order unchanged — only this one pass needs the extra arg) — selects the main-vs-default launch-ack replacement wording, see `strip_bg_launch_ack.py`. Default `""` (`is_main=False`) preserves the exact pre-2026-08-06 wording for every caller not yet updated to pass `worker_context` (only `addon.py` passes it in production). **Second job for the same param (2026-08-29):** it is forwarded as `_load_system2_rules(model_family, project_path, worker_context)`, where it selects the system2 rule set by session role — see `rules_config.py`. Both jobs read the same string but on different predicates: rule selection tests `startswith("worker:")`, the launch-ack wording tests `== "main"`, so a caller passing neither (or `None`) gets main rules with the default wording. **(2026-09, helper-extraction milestone)** `apply_modification_rules` (was 62 LOC) split into `_run_message_passes(messages_to_process, passes)` — the `_passes` loop + `modifications`/`stripped_msg_indices`/`stripped_msg_originals`/`stripped_msg_removed`/`injected_msg_added`/`all_ops` accumulation `apply_modification_rules` used to inline; `apply_modification_rules` itself now builds the `_passes` list, calls `_run_message_passes`, then `_dedup_wakeup_blocks` + `_apply_system_passes`.
**Reads:** Raw payload dict; rule text via `rules_config._load_system2_rules`.
**Writes:** Nothing — returns `(modified_payload, modifications, original_system2_text, stripped_msg_indices, stripped_msg_originals, stripped_msg_removed, injected_msg_added, _all_ops)` 8-tuple. `_all_ops` is `{msg_idx: {blk_idx: [(offset, removed, injected)]}}` — position-anchored ops from all passes accumulated via `_merge_ops`.
**Called by:** `src/proxy/addon.py` (imports `apply_modification_rules`, `_strip_blocked_tool_references`)
**Calls out:** `rules_config`, `message_passes`, `message_passes_simple`, `message_passes_wakeup`, `rule_ops` (`_merge_ops`), `content_strip` (`_strip_session_guidance`, `_strip_git_status`), `payload_helpers` (`_strip_blocked_tool_references` re-export).
New private helpers (same module): `_run_message_passes` (2026-09).

---

### rule_ops.py (93 LOC)

**Purpose:** Op-recording primitives shared by all message passes. `_extract_block_op(before, after, full_replace=False)` — minimal `(offset, removed, injected)` triple via common-prefix/suffix scan; `full_replace=True` (2026-07-29 milestone-3) skips the trim entirely and returns `[(0, before, after)]` — a caller-supplied signal, never inferred (the function only ever sees two strings, and the measured FULL/PARTIAL size-ratio ranges overlap, so no threshold could discriminate the two cases — see `process-docs/proxy_instrumentation/`). `_block_inner_text(block)` — extracts plain text from any content block shape (str, text-dict, tool_result, fallback json.dumps). `_ops_from_content_change(old, new, full_replace=False)` — per-block ops dict `{blk_idx: [(offset, removed, injected)]}` dispatching over THREE content-shape transitions (list→list, **list→str**, str→str), threading `full_replace` straight through to `_extract_block_op` for every block/string pair. **The mixed list→str branch was added 2026-08-30 and is a logging-attribution fix, not a wire change:** a pass that collapses block content to a bare string (live instance: `_apply_role_system_strip` setting content to `"."`) matched neither of the two original branches and got back `{}`, so `strip_inject_delta._process_messages_section`'s `if s_texts:` wrote no stripped span for that request. The strip then surfaced one request LATE, attributed to whichever request re-sent the message as a plain string — measured before the fix: 0 of 510 trailing total_tokens strips recorded against the request that performed them. The trigger is CC hanging the cache-control breakpoint on the last message, which makes an otherwise-plain system message arrive list-shaped. The new string becomes block 0's after-text, matching `_process_messages_section`, which recomputes each block's before-text from the ORIGINAL content; further blocks are recorded as removed for completeness but nothing consumes them, because `diff_engine._diff_messages` emits a SINGLE block diff for a mixed shape (no multi-block list→str collapse occurs in any recorded session). `_merge_ops(dst, src)` — accumulates per-pass ops into `_all_ops` in the orchestrator.
**Reads:** Nothing — operates on content values passed as arguments.
**Writes:** Nothing — returns new dicts/lists/tuples; no mutation of arguments.
**Called by:** `src/proxy/message_passes.py`, `src/proxy/message_passes_simple.py`, `src/proxy/message_passes_wakeup.py` (all three import `_ops_from_content_change`); `src/proxy/rules.py` (imports `_merge_ops`)
**Calls out:** —

---

### message_passes.py (305 LOC, split by concern 2026-09 — see `message_passes_simple.py`/`message_passes_wakeup.py` entries below and `process-docs/proxy/` for the split methodology)

**Purpose:** The 4 STRUCTURAL message-level passes — each with its own logic, not reducible to the generic template `message_passes_simple.py` owns. Each receives `messages: list` and returns the standard 6-tuple `(new_messages, pass_mods, pass_removed_by_idx, changed_indices, pass_injected_by_idx, pass_ops_by_msg_blk)`. Pass sequence (call order in `rules._passes` loop; the 8 template passes and `_dedup_wakeup_blocks` interleave with these — see the other two modules' entries for their own position and behavior): `_apply_role_system_strip` (replaces entire content of every `role='system'` message with `"."`, mod: `stripped_role_system_msg` — records its op with `full_replace=True` (2026-07-29 milestone-3, `rule_ops.py`): content is set to the literal `"."` unconditionally, an unambiguous whole-content replacement, so the op is one contiguous `(0, before, ".")` with no prefix/suffix trim; CC 2.1.176 delivers deferred-tools / agent-types / skills as a role=system plain-string message on Opus; idempotency guard skips empty + already-`"."` content; **preserve-guard (CC 2.1.205+):** a `role='system'` message whose `content` is a `str` starting with `_TRUNCATION_NOTICE_MARKER = "[Truncated:"` is appended unchanged instead of nuked — this is CC's Read-partial-view notice (`[Truncated: PARTIAL view — <path>: showing lines X-Y of Z total …]`), and stripping it would leave the agent unaware a Read was incomplete. `startswith`, not substring — anchored to avoid false-positiving on content that merely quotes the marker. Delivered exclusively as a standalone string content role=system message, never embedded in tool_result, per dual-log analysis; **TN preserve-guard (2026-07-29):** a `role='system'` message whose content top-level-contains `<task-notification>` is also appended unchanged instead of nuked — CC delivers bg-task wake-ups as a plain-str role='system' message on some paths (measured: 173/280 real TN occurrences in one session log were role='system'/str vs 107 role='user'/list-text); left untouched here so `_apply_sn_notice_strip` (`message_passes_simple.py`) + this module's own `_apply_first_pass`'s TN branch (both widened to accept role='system', narrowly gated on the TN tag) build the wake-up exactly once — no duplicated TN-construction logic); **mid-turn-user-message preserve-guard (2026-08-07, issue #61):** a `role='system'` message whose `content` is a `str` and whose `content.lstrip()` starts with `_MID_TURN_USER_MSG_MARKER = "The user sent a new message while you were working:"` is also appended unchanged instead of nuked — CC 2.1.223 delivers a mid-turn user message (user typing while the session works, or `worker-cli send` to a working worker — same mechanism) as a role='system' plain-string message; pre-223 the identical content arrived as a role='user' `<system-reminder>` (`strip_sr.py`'s `'user-interrupt'` template, PARTIAL mode — IMPORTANT line stripped, user body kept), a guard this pass's blanket role='system' nuke bypassed entirely, silently dropping the user's own text before it ever reached the model. Whole-message preserve (not a partial trim like the SR-era guard) — losing the user's text is the failure mode, the boilerplate explainer tail is harmless. `changed_indices`/`pass_removed_by_idx`/`pass_ops_by_msg_blk` all skip this message exactly like the other two preserve-guards, so downstream (`strip_inject_delta.py`'s `'RS'` attribution, gated on `if s_texts:`) sees no diff at all — no adjustment needed there, confirmed by design); `_apply_first_pass` (elif-chain: task-notification tags + wakeup injection, task-tools-nag SR, deferred-tools SR, user-interrupt SR, rejection message — TN branch: both completed and failed call `_replace_task_notification_tags` inline, identical output logic, mod-name only differs: `trimmed_task_notification` for completed, `replaced_task_notification` for failed; **injected_text is up to 3 lines (2026-07-29):** wake-up line, then `Output: <path>` (only if `_extract_task_notification_output_file` returns non-empty), then `ID: <task-id>` (only if `_extract_task_notification_task_id` returns non-empty) — built as a `lines` list joined with `\n` + trailing `\n`, so the neither-present case collapses back to exactly `_WAKEUP_TEXT` (no special-casing needed); order is fixed regardless of which optional lines are present; **TN branch role gate accepts `role in ('user', 'system')`** (2026-07-29, same TN wake-up fix) — every other elif branch (nag/deferred/user-interrupt/rejection) stays `role == 'user'`-only, no measured role='system' occurrence for those; **the task-tools-nag/deferred-tools/user-interrupt branches gate via `_top_level_content_contains`, not `_content_contains`** — 2026-07-28 FP-nuke fix, defense-in-depth alongside `strip_sr.py`'s own tool_result non-descent, same pattern as `_apply_sn_notice_strip`/`_apply_bg_exit_strip` (`message_passes_simple.py`); the rejection branch is unrelated — `_message_has_rejection` genuinely only ever checks inside tool_result, own marker + length guard, not part of the SR family; **records its op with `full_replace=True`** (2026-07-29 milestone-3) despite `_strip_rejection_message` (`content_strip.py`) mutating PER-BLOCK for list content rather than the whole message at once — safe specifically because every block it actually changes is wholesale-set to the literal `"."` (`content_strip.py:43`), never partially edited, and every block it leaves alone is appended by identity (`content_strip.py:45`) so `bt==at` there and `_extract_block_op`'s `before==after` check returns `[]` before `full_replace` is even consulted; the str branch (`content_strip.py:31`) is unambiguously full-replace too); `_apply_cumulative_sr_strips` (skills-SR, agent-types-SR, claudeMd-SR, pyright-diagnostics — cumulative: applied to every user msg including those already touched by pass 1, diff-based SR extraction; `stripped_agent_types_sr` added for Sonnet-worker standalone agent-types SR ~2,353 chars; **all 4 gates use `_top_level_content_contains`**, same 2026-07-28 fix); `_apply_final_sr_pass` (strips all remaining `<system-reminder>` blocks from top-level content only — no gate of its own, so `strip_sr.py`'s tool_result non-descent is the only thing protecting tool_result content here). All passes call `_ops_from_content_change` from `rule_ops`.

**`_apply_first_pass`'s TN branch calls `message_passes_wakeup._unwrap_full_sr_wrapper(content)` (2026-09-04)** — strips a top-level `<system-reminder>` wrapper CC (2026-09+) sometimes puts around BOTH the SN-notice paragraph AND the `<task-notification>` tag in one block (previously always arrived as one or the other, unwrapped); `_apply_sn_notice_strip`'s anchored `lstrip().startswith()` check misses the paragraph in this shape (the wrapper, not the paragraph, sits at position 0), so without this the wrapper survives the TN-tag replace and `_apply_final_sr_pass` later full-strips the ENTIRE `<system-reminder>` block — wrapper AND the just-injected wake-up text — since the paragraph-prefixed inner text matches `strip_sr.py`'s `'system-notification'` template (mode `'full'`); measured 20/22 wake-ups lost this way in one real session (`api_requests_opus_monitor_cc_1788464543`, 2026-09-03/04). See `message_passes_wakeup.py`'s own entry for `_SR_FULL_WRAP_RE` and the full mechanics.

**(2026-09, helper-extraction milestone) `_apply_first_pass` (was 100 LOC, HARD) split into one handler per elif branch:** `_handle_tn_message` (the TN branch, ~30 LOC on its own — the biggest single branch, per the milestone's own hint), `_apply_sr_marker_branch` (shared by the NAG and DEF branches, which strip a single `<system-reminder>` by marker via `_strip_system_reminder` and differ only in marker/mod_name), `_handle_ui_message` (user-interrupt partial-mode branch), `_handle_rejection_message` (rejection branch); `_commit_simple_branch` folds the "append to `changed_indices`/`pass_mods`/`pass_removed_by_idx`/`pass_ops_by_msg_blk` only if changed" tail every non-TN branch repeated into one call, threading a shared `acc` dict so `_apply_first_pass` itself stays a thin elif-dispatch loop. `_apply_cumulative_sr_strips` (was 53 LOC) split via `_apply_marker_sr_strip` (the 3 identical-shape Skills/agent-types/claudeMd checks, looped over the new `_CUMULATIVE_SR_MARKERS` module constant) — the 4th check (pyright, config-gated + a different strip function) stays inline. Byte-identical — verified via `dev/proxy/pipeline_byte_identity.py`.
**Reads:** Message list; `rules_config._load_config()` (pyright-strip flag in `_apply_cumulative_sr_strips`).
**Writes:** Nothing — returns new lists/dicts; no mutation of input messages.
**Called by:** `src/proxy/rules.py` (imports `_apply_role_system_strip`, `_apply_first_pass`, `_apply_cumulative_sr_strips`, `_apply_final_sr_pass`)
**Calls out:** `strip_sr`, `content_strip` (`_message_has_rejection`, `_strip_rejection_message`), `payload_helpers`, `rules_config`, `strip_bg_completed` (`_WAKEUP_TEXT`), `rule_ops`, `message_passes_wakeup` (`_unwrap_full_sr_wrapper`)
New private helpers (same module): `_apply_marker_sr_strip`, `_handle_tn_message`, `_apply_sr_marker_branch`, `_handle_ui_message`, `_commit_simple_branch`, `_handle_rejection_message`.

---

### message_passes_simple.py (184 LOC, new 2026-09, split out of `message_passes.py`)

**Purpose:** The generic pass runner + one declarative spec per pass — the ONE concern 8 of the 12 original message passes shared: role filter → marker-contains guard → `strip_fn(content) -> (new_content, removed)` → on removal record mod name / changed idx / removed / optional injected / ops (with an optional `full_replace` flag). `_run_simple_pass(messages, spec)` is the runner; `spec` fields: `roles` (frozenset), `skip_fn` (optional extra per-role skip — `_sn_notice_skip` only), `marker_guard`, `strip_fn`, `mod_name`, `injected` (optional static list, a FRESH copy stored per hit), `full_replace`. `_any_marker_guard(check_fn, markers)` builds an "any of N markers" guard (bd_noise, interrupt_marker, bg_launch_ack's two wordings). The 8 passes, each a spec dict + a thin `def _apply_*_strip` wrapper calling `_run_simple_pass`:
- `_apply_po_preview_strip` — strips `Preview (first NKB):` section from `<persisted-output>` blocks, mod: `stripped_po_preview`.
- `_apply_bg_exit_strip` — replaces first BGK kill notification with `_WAKEUP_TEXT` (id-less, path-less by design), mod: `replaced_bg_completed_text`, `injected: [_WAKEUP_TEXT]`.
- `_apply_hook_prefix_strip` — strips `PreToolUse:<Tool> hook error: [python3 <path>]:` prefix from tool_result content, mod: `stripped_hook_error_prefix`.
- `_apply_git_lock_strip` — strips constant 5-line git index.lock advice from tool_result content, mod: `stripped_git_lock_advice`.
- `_apply_bg_launch_ack_strip(messages, is_main=False)` — replaces block content with a 3-line hold message (see `strip_bg_launch_ack.py`) for blocks whose text `lstrip().startswith()` one of TWO anchored prefixes (wording 1 `'Command running in background with ID:'`, wording 2 `'Command was manually backgrounded by user with ID:'`); fast-path gate ORs the two corresponding markers via `_any_marker_guard`; mod: `stripped_bg_launch_ack`, `full_replace: True`. `is_main` threads through via a per-call spec copy (`spec = dict(_BG_LAUNCH_ACK_SPEC); spec["strip_fn"] = lambda c: _strip_bg_launch_ack(c, is_main)`) rather than widening the runner's own signature — the one pass needing an extra argument; caller is `rules.py`'s `_bg_launch_ack_pass` closure.
- `_apply_bd_noise_strip` — strips bd informational auto-import/export lines from tool_result content, mod: `stripped_bd_noise`, marker guard over `_BD_NOISE_MARKERS` via `_any_marker_guard`.
- `_apply_interrupt_marker_strip` — replaces block content with `'.'` for any block whose whitespace-stripped text IS EXACTLY one of two wordings (`_INTERRUPT_MARKERS`, via `_any_marker_guard`) — CC's rendering of `bg_escape.py`'s tmux-Escape into a worker's pane, never a genuine user interrupt; mod: `stripped_interrupt_marker`, `full_replace: True`.
- `_apply_sn_notice_strip` — strips the bare 4-line `[SYSTEM NOTIFICATION - NOT USER INPUT]` paragraph CC injects ahead of `<task-notification>` tags in bg-task wake-ups, anchored `lstrip().startswith()` decision on top-level str/text-block content only, does NOT descend into tool_result; runs BEFORE `_apply_first_pass` (`message_passes.py`) so the TN branch's tag-contains guard and tag-replacement operate on an already-cleaned prefix; mod: `stripped_sn_notice_paragraph`. The ONE spec with a non-trivial `roles`/`skip_fn`: `roles = {"user", "system"}`, `skip_fn = _sn_notice_skip` (`role == "system" and not top-level-TN-tag` → skip) — a role='system' message without a TN tag stays out of scope even when the pass is called in isolation, defensive boundary not just a pipeline-order fact.

All 8 records its op via `_ops_from_content_change` (`rule_ops.py`), same as the structural passes in `message_passes.py`.
**Reads:** Message list.
**Writes:** Nothing — returns new lists/dicts; no mutation of input messages.
**Called by:** `src/proxy/rules.py` (all 8 functions imported, wired into the `_passes` list at their original pipeline positions)
**Calls out:** `payload_helpers` (`_content_contains`, `_top_level_content_contains`), `rule_ops` (`_ops_from_content_change`), `strip_po`, `strip_bg_completed`, `strip_bg_launch_ack`, `strip_hook_prefix`, `strip_git_lock`, `strip_bd_noise`, `strip_sn_notice`, `strip_interrupt_marker`

---

### message_passes_wakeup.py (95 LOC, new 2026-09, split out of `message_passes.py`)

**Purpose:** The wake-up concern — `_dedup_wakeup_blocks` and `_unwrap_full_sr_wrapper`, both operating on already-injected wake-up text rather than on original CC content the way every pass in the other two modules does. `_dedup_wakeup_blocks(messages) -> (new_messages, ops_by_msg_blk)` — deduplicates multiple `_WAKEUP_TEXT` injections within the same user message to one (keeps first occurrence); called AFTER the full `_passes` loop by `rules.apply_modification_rules`, not part of `_passes` itself. TN path appends `{text: _WAKEUP_TEXT}` with a trailing `\n`; BGK path (`_apply_bg_exit_strip`, `message_passes_simple.py`) inlines via `_strip_bg_from_text` which calls `.strip()`, producing `_WAKEUP_TEXT.rstrip('\n')` — comparison uses `rstrip('\n')` so both forms count as one wake-up. Inject points across passes, for reference: (1) TN branch (both failed + completed, role='user' or role='system') → `[_WAKEUP_TEXT]` or the up-to-3-line variant (`message_passes.py`); (2) `_apply_bg_exit_strip` when `bg_removed` non-empty → `[_WAKEUP_TEXT]` (id-less/path-less).

`_unwrap_full_sr_wrapper(content)` — strips a top-level `<system-reminder>` wrapper (and any SN-notice paragraph inside it) around already-TN-processed content — see `message_passes.py`'s own entry for the CC-2026-09+ shape this exists for and the measured wake-up-loss it fixes. `_SR_FULL_WRAP_RE = r'\A<system-reminder>\n(.*)</system-reminder>\s*\Z'` (DOTALL) — anchored to the ENTIRE str, or a single `list[text]` block's own full text, never substring-anywhere (same FP-nuke-class guard as `strip_sn_notice.py`); called AFTER `_replace_task_notification_tags` (the TN tag is already gone, so this only needs to detect+remove the wrapper) and reuses `_strip_sn_notice` (imported from `strip_sn_notice.py`) for the paragraph removal on the unwrapped inner text. No-op — byte-identical output — for the unwrapped shape (content doesn't start with `<system-reminder>`) and for every other content shape (tool_result never descended into, multi-block content where the tag block isn't itself a whole SR wrap). Returns `(new_content, removed_chunks)`; `removed_chunks` feeds into `_handle_tn_message`'s (`message_passes.py`) existing `pass_removed_by_idx` list.
**Reads:** Message list / content values passed as arguments.
**Writes:** Nothing — returns new lists/dicts/tuples; no mutation of input.
**Called by:** `src/proxy/rules.py` (`_dedup_wakeup_blocks`, called after the `_passes` loop); `src/proxy/message_passes.py` (`_unwrap_full_sr_wrapper`, from `_handle_tn_message`)
**Calls out:** `strip_sn_notice` (`_strip_sn_notice`), `strip_bg_completed` (`_WAKEUP_TEXT`), `rule_ops` (`_ops_from_content_change`)

---

### cache.py (158 LOC)

**Purpose:** Strip all existing cache_control markers from a payload and place new breakpoints (Tools Anchor, Tools End, BP3 last-unchanged-msg, BP4 last-msg). **(2026-09, helper-extraction milestone) `_set_cache_breakpoints` (was 56 LOC) split into one function per breakpoint** — `_apply_bp1_system`, `_apply_bp2_tools` (both mutate `result` in place, return whether they fired), `_apply_bp3_unchanged_tail`, `_apply_bp4_last_message` (both take/return `messages` since BP4 must see BP3's own edit) — `_set_cache_breakpoints` itself is now 4 sequential calls threading `bp_count` (computed but never read downstream — pre-existing, unchanged).
**Reads:** Payload dicts; previous request's message summaries (for BP3 unchanged-prefix detection); previous tools count (for anchor-on-growth logic).
**Writes:** Nothing — returns modified payload dicts.
**Called by:** `src/proxy/addon.py`
**Calls out:** —
New private helpers (same module): `_apply_bp1_system`, `_apply_bp2_tools`, `_apply_bp3_unchanged_tail`, `_apply_bp4_last_message`.

---

### strip_sr.py (186 LOC, `_ENV_CONTEXT_RE` widened 2026-09 for CC 2.1.258)

**Purpose:** Strip `<system-reminder>` tag blocks from API message content via template-based exact-match. Maintains a catalog of 11 known SR templates (task-tools-nag, pyright-new-diagnostics, deferred-tools, user-interrupt, system-notification, file-modified, claudemd-contents, date-changed, skills-available, agent-types, plan-mode); each template has one or more identifier strings. `claudemd-contents` uses a list of identifiers (`"As you answer the user's questions"` for CC's preamble form, `"Contents of "` for the bare form) — `_match_template` iterates the list with OR semantics. Strip uses `startswith` against extracted SR-block inner text — no greedy regex across code literals. `<task-notification>` blocks do NOT go through this module — they are handled separately by `_apply_first_pass` in `message_passes.py`. The injected `_WAKEUP_TEXT` is plain text — no `<system-reminder>` tags, SR-strip passes uninvolved. `_apply_sr_strip._replace` has a pre-guard `_ENV_CONTEXT_RE.fullmatch(inner)` check that fires BEFORE the `_PRESERVE_PREAMBLE` guard, stripping CC's injected userEmail/currentDate SR block; the full-block match (email literal + date regex + IMPORTANT footer literal) ensures CLAUDE.md-context blocks with the same preamble are never false-positively stripped. **CC 2.1.258 trailing-sentences fix (2026-09):** CC appends 2 sentences after the email address on the SAME line before `# currentDate` ("Use it only to identify the user, ... Never send it to an unrelated service, ..."); the old regex required `\n` immediately after `gmail\.com\.`, so `fullmatch` failed and the block reached the API in message 0 of every session (`SUS:<SR>` in the proxy pane). Fix: `[^\n]*` after the email sentence tolerates any trailing text on that one line, matching both the pre-2.1.258 form and the 2.1.258 form with one pattern. **Measured over `src/logs/dual_log/*_original.jsonl` (main checkout, 2026-09, 14 files, 2888 entries, deduplicated by (file, exact text)):** 3 distinct blocks were genuinely broken by CC 2.1.258 (no `# claudeMd`, `fullmatch` failed pre-fix) — all 3 now strip. A separate, UNRELATED real shape exists where CC bundles `# claudeMd` project content AND `# userEmail`/`# currentDate` into ONE `<system-reminder>` block (3 distinct blocks in this corpus) — `fullmatch` correctly never matches these (the inner text is not JUST the env-context block), so they stay preserved via `_PRESERVE_PREAMBLE` both before and after the fix; this is by design, not a residual bug — losing real CLAUDE.md content would be worse than leaving the small env-context addendum unstripped inside an otherwise-preserved block. Replay probe: `dev/proxy/replay_env_context_strip.py`, report `dev/proxy/md/replay_env_context_strip.md`. **Partial-strip trailing-`\n`:** `_apply_sr_strip._replace` partial path preserves the original trailing-`\n` state: `trailing_nl = '\n' if full.endswith('\n') else ''` — the `\n` is appended only when the matched original had one. When the original SR had no trailing newline, `_STANDALONE_SR_RE` (ends with `\n?`) consumed none, so appending unconditionally would introduce a net-new `\n` into the forwarded payload; with the fix the output is byte-identical to the input w.r.t. the trailing character. **`_strip_system_reminders` no longer descends into `tool_result` (2026-07-28 FP-nuke fix — see `process-docs/message_strip_fp_nuke/2026-07-28_tool_result_sr_audit.md`):** only `str` content and top-level `list[type=='text']` blocks are scanned; any `tool_result` block (str or list-of-text content) is returned by IDENTITY, not rebuilt. Prior behavior FP-nuked quoted SR examples inside RAG/Read/gh tool_results (a `rag-cli` search returning a process-docs excerpt that fences a literal env-context SR block got stripped as if genuinely CC-injected). Measured over ~660 real requests across 5 sessions: 0 genuine SR-family occurrences inside `tool_result`, 1 confirmed false positive — the entire family's `tool_result` descent carried no value. This is a scope reduction, not a disable: top-level SR stripping is unaffected.
**Reads:** Message content (string or list of blocks); template catalog (module-local).
**Writes:** Nothing — returns modified content.
**Called by:** `src/proxy/message_passes.py`
**Calls out:** stdlib only (`re`).
---

### strip_po.py (72 LOC)

**Purpose:** Strip the `Preview (first NKB):` section from `<persisted-output>` blocks injected by CC when Bash output exceeds its inline limit. Preserves the `<persisted-output>` wrapper and the `Output too large ... Full output saved to:` header line; removes only the Preview section (which biases readers toward 2KB snippets rather than the persisted file). Traverses all 4 content shapes (top-level string, list→text, list→tool_result/string, list→tool_result/list-of-text) mirroring `strip_sr.py`. Malformed PO blocks (missing `Output too large` or `Preview (first` header) are left untouched. Returns `(new_content, removed_chunks)` — caller (`rules.py` PO-Preview pass) appends chunks to `stripped_msg_removed` for `attribute_chunk` PP-rule attribution.
**Reads:** Message content (string or list of blocks).
**Writes:** Nothing — returns `(modified_content, list[str])`.
**Called by:** `src/proxy/message_passes_simple.py` (`_apply_po_preview_strip`)
**Calls out:** stdlib only (`re`).

---

### strip_bg_launch_ack.py (107 LOC)

**Purpose:** Replace entire block content with an instructive 3-line hold message for genuine background-command launch-ack blocks. **Context-selected hold-message wording (2026-08-06 milestone-2):** `_strip_bg_launch_ack(content, is_main=False)` / `_build_launch_ack_replacement(ack_text, is_main=False)` pick `_BG_LAUNCH_ACK_MSG_MAIN` (explicit "go idle now and wait ... completion notice for this exact task ID") when `is_main=True`, else the original `_BG_LAUNCH_ACK_MSG` (unchanged text, still what every worker context and every un-updated caller gets — default `False`). Only the intro line differs; the `Output:`/`ID:` lines are shared, built the same way regardless of context. `is_main` is threaded in from `rules.py`'s `worker_context` param via `message_passes.py`'s `_apply_bg_launch_ack_strip`. **Two wordings recognized (2026-07-29 milestone-2):** wording 1 — CC emits `"Command running in background with ID: <id>. Output is being written to: <path>. You will be notified when it completes. To check interim output, use Read on that file path."` on initial background launch; wording 2 — `"Command was manually backgrounded by user with ID: <id>. Output is being written to: <path>"` (no trailing sentence, ack can BE the complete block) when the USER manually backgrounds an already-running Bash call. **Anchored decision** — `_is_bg_launch_ack(text)` returns True only when `text.lstrip().startswith(_BG_LAUNCH_ACK_PREFIX)` OR `startswith(_BG_LAUNCH_ACK_PREFIX_2)` — two full-sentence literal prefixes, each still as specific/narrow as before, OR'd rather than widened to one shared broad substring (a broad marker like `"with ID:"` would reopen the FP-nuke class of bug this anchor exists to prevent). A genuine ack ALWAYS starts with one of these prefixes; a large tool_result or a user-typed/pasted message that merely CONTAINS either phrase as data mid-content is preserved. `_BG_LAUNCH_ACK_MARKER`/`_BG_LAUNCH_ACK_MARKER_2` are retained only as the cheap fast-path gate imported by `message_passes.py` (OR'd there too — GATES whether the strip is invoked; the actual FP-safety decision lives in the anchored prefix check, not the gate). **Replacement (2026-07-29, 3 lines, same shape for both wordings):** `_build_launch_ack_replacement(ack_text)` recovers the id and output path FROM the ack text being replaced and emits: hold-instruction line (identical regardless of source wording), then `Output: <path>` (only if extracted), then `ID: <id>` (only if extracted) — a missing value OMITS its line, never `ID: None`. `_ACK_ID_RE` is anchored on the `"with ID:"` fragment common to BOTH wordings (not the wording-specific full prefix) — safe only because this regex is exclusively reachable from `_build_launch_ack_replacement`, which every call site gates behind `_is_bg_launch_ack` first; the FP guard lives entirely in that anchor, not in the extraction regexes. `_ACK_PATH_RE` is one alternation-based regex (not two wording-specific ones): non-greedy up to whichever comes first — the wording-1 terminator (`". You will be notified"`), a literal newline, or end-of-string. **The newline bound (2026-07-29) is load-bearing, not decorative:** without it, a wording-2 ack followed by trailing content in the SAME block (unobserved in the measured corpus, but the `_ops_from_content_change` blast-radius classification of this exact pass already establishes "ANY trailing content after the ack in that block is also discarded" by the replacement, so it is a real, possible shape) would have that trailing content swallowed into the `Output:` line instead of being cleanly discarded — pinned as a regression guard (`W24`, real ack + trailing note, asserts the trailing text is absent and the path is exact). `re.DOTALL` is kept (needed only by the wording-1 branch — the terminator sentence is matched via `\s*`, not `.`; DOTALL exists for a hypothetical internal newline before the real path, not exercised by real data) and does not reintroduce the swallowing risk, since the wording-1 terminator branch is tried first in the alternation and always wins before any embedded newline is reached in every measured wording-1 path. Verified against real recorded acks (`src/logs/dual_log/`) and the live-observed wording-2 text — id+path present in every measured occurrence of both wordings; the omit-path is defensive, exercised only by synthetic fixtures. Same fixed line order (`Output:` before `ID:`) as the TN termination path in `message_passes.py`'s `_apply_first_pass` — mirrors that pass's shape intentionally, no shared code (each builds its own 3 lines from its own extraction). Covers all 4 content shapes (str, list/text, list/tool_result-str, list/tool_result-list). Does NOT match BGK completion notification (`"Background command … failed/completed"`) — `strip_bg_completed.py` is untouched by this change, still id-less/path-less by design. Returns `(new_content, removed_chunks)` for `stripped_bg_launch_ack` mod attribution via BL rule (both markers registered in `strip_vocab.py`'s `RULES['BL']`); `removed_chunks` still holds the original, unmodified ack text.
**(2026-09, helper-extraction milestone) `_strip_bg_launch_ack` (was 50 LOC) delegates to the shared `payload_helpers._walk_replace_marker_blocks(content, predicate, replace_fn)` walker** — folded together with `strip_interrupt_marker.py`'s own `_strip_interrupt_marker`, whose walk was byte-identical in shape (str → predicate+replace; list → text/tool_result-str/tool_result-list, same `sub_changed` guard), verified before folding; only the predicate (`_is_bg_launch_ack`) and replace_fn (`lambda text: _build_launch_ack_replacement(text, is_main)`) differ.
**Reads:** Message content (string or list of blocks).
**Writes:** Nothing — returns `(modified_content, list[str])`.
**Called by:** `src/proxy/message_passes_simple.py` (`_apply_bg_launch_ack_strip`)
**Calls out:** `payload_helpers` (`_walk_replace_marker_blocks`)

---

### strip_bg_completed.py (71 LOC)

**Purpose:** Replace the first background-Bash kill notification in user-turn content with a generic plain-text wake-up hint; strip any further ones. CC injects `Background command “…” failed/completed (exit code 143/137)` when a background Bash process is terminated via SIGTERM (143) or SIGKILL (137). Instead of silently removing this, the proxy repurposes it: the first match is replaced with `_WAKEUP_TEXT` (generic wake-up hint), subsequent duplicates in the same traversal are stripped. Traverses top-level string and top-level `text` blocks only — does NOT descend into `tool_result` (defense in depth: guard in `rules.py` already uses `_top_level_content_contains`; not traversing tool_result here closes the gap structurally). Does NOT match exit code 0 — that is the legitimate timer-done polling signal. Returns `(new_content, removed_chunks)` for `replaced_bg_completed_text` mod attribution.

**Wake-up architecture:** CC natively injects bg-task-completion signals when a background bash timer ends or is killed. Rather than discarding them (leaving Opus with an empty wake-up turn and no context), the proxy replaces the first signal with a plain-text hint. Covers both worker-idle wake-ups and any other backgrounded task completion (rag-cli, build, etc.). Plain text avoids `<system-reminder>` wrapping and the associated SR-strip-pass concerns.
**Reads:** Message content (string or list of blocks).
**Writes:** Nothing — returns `(modified_content, list[str])`.
**Called by:** `src/proxy/message_passes_simple.py` (`_strip_bg_exit_notifications`/`_BG_CMD_MARKER`/`_WAKEUP_TEXT`, for the `_apply_bg_exit_strip` spec); `src/proxy/message_passes.py` (`_WAKEUP_TEXT`, in `_handle_tn_message`); `src/proxy/message_passes_wakeup.py` (`_WAKEUP_TEXT`, in `_dedup_wakeup_blocks`)
**Calls out:** stdlib only (`re`).
---

### strip_hook_prefix.py (68 LOC)

**Purpose:** Strip CC's hook-error wrapper prefix (`PreToolUse:<Tool> hook error: [python3 <path>]:`) from user-turn `tool_result` content before forwarding to Anthropic. CC wraps every hook stderr in this prefix; the actual user-visible error message follows it. Stripping at proxy level means Anthropic receives the clean message, and `_build_entry` logs the stripped version — downstream display (warnings pane) reads clean `full_text` with no further transformation needed. Returns `(new_content, removed_chunks)` for `stripped_hook_error_prefix` mod attribution. Fast-path guard `_HOOK_PREFIX_MARKER = 'PreToolUse:'` skips non-matching messages cheaply. Traverses all 4 content shapes mirroring `strip_po.py`.
**Reads:** Message content (string or list of blocks).
**Writes:** Nothing — returns `(modified_content, list[str])`.
**Called by:** `src/proxy/message_passes_simple.py` (`_apply_hook_prefix_strip`).
**Calls out:** stdlib only (`re`).

---

### strip_git_lock.py (71 LOC)

**Purpose:** Strip the constant 5-line git index.lock advice block from user-turn `tool_result` content before forwarding to Anthropic. CC appends this block (hardcoded in git's `lockfile.c`) to bash tool output when the beads auto-export hook calls `git add` while another git process holds `index.lock`. The variable warning line above it (`Warning: auto-export: git add failed: … File exists.`) is preserved — it contains the actionable repo path. Exact literal match via `str.replace` (no regex needed — block is constant across all repos/versions). Fast-path guard `_GIT_LOCK_MARKER = 'Another git process seems to be running'` skips non-matching messages cheaply. Returns `(new_content, removed_chunks)` for `stripped_git_lock_advice` mod attribution. Traverses all 4 content shapes mirroring `strip_hook_prefix.py`.
**Reads:** Message content (string or list of blocks).
**Writes:** Nothing — returns `(modified_content, list[str])`.
**Called by:** `src/proxy/message_passes_simple.py` (`_apply_git_lock_strip`).
**Calls out:** stdlib only.

---

### strip_bd_noise.py (91 LOC)

**Purpose:** Strip all bd informational auto-import/export lines from user-turn `tool_result` content before forwarding to Anthropic. bd (beads) emits these on every git commit via its auto-export hook: import-start (`auto-importing N bytes … into empty database`), import-done (`auto-imported N issues [and N memories] from …` / `auto-imported N issues into empty database`), export-done (`Exported N issues … to …` / `auto-export: wrote …`), and no-op status lines (`auto-export: no changes since last export` / `auto-export: throttled …` / `auto-export: skipping …`). Also covers upgrade-recovery variants (`auto-import: N issues from …`). All variants stripped with and without the `- ` bullet prefix present in hook-captured output. `Warning:` / `warning:` prefixed error lines (`auto-export failed:`, `auto-export skipped:`, `auto-import: failed to parse …`) are NOT matched — regex starts at `auto-import`/`auto-export`/`Exported`, never `Warning:`. Fast-path markers: `_BD_NOISE_MARKERS = ('issues.jsonl', 'auto-export:', 'into empty database')` — three needed because `auto-imported N issues into empty database` contains neither of the first two. Returns `(new_content, removed_chunks)` for `stripped_bd_noise` mod attribution. Traverses all 4 content shapes mirroring `strip_git_lock.py`.
**Reads:** Message content (string or list of blocks).
**Writes:** Nothing — returns `(modified_content, list[str])`.
**Called by:** `src/proxy/message_passes_simple.py` (`_apply_bd_noise_strip`).
**Calls out:** stdlib only (`re`).

---

### strip_interrupt_marker.py (43 LOC)

**Purpose:** Replace block content with `'.'` for any block whose whitespace-stripped text IS EXACTLY one of two wordings — `"[Request interrupted by user]"` or `"[Request interrupted by user for tool use]"` — CC's rendering of `bg_escape.py`'s tmux-Escape sent into a worker's pane on a backgrounded call, indistinguishable in CC's own log from a genuine user Ctrl-C/Escape interrupt, though no user ever pressed anything. A worker reading the marker halts and waits for an instruction nobody intended to give (measured live: worker replied "I was interrupted right at the point of pivoting to Step 2 … Waiting for the orchestrator's next instruction."). **Whole-block match ignoring only surrounding whitespace** (`text.strip() in _INTERRUPT_MARKERS`), NOT substring-anywhere — re-measured (2026-07-31, deduped to the last/fullest line per `src/logs/dual_log/*_original.jsonl` session file, since a raw per-line scan overcounts ~150x on the accumulating message history each request re-embeds): 11 occurrences across 5 session files, ALL `role='user'`, block `type='text'`, EVERY occurrence trailing-`\n`-terminated (10x base wording, 1x "for tool use"), block position varying (index 1 of 3, 1 of 5, or 0 of 2), never embedded inside longer text — the anchor exists so a genuine user paste/transcript that happens to CONTAIN this phrase inside longer text is never nuked (same FP-nuke class as `strip_bg_launch_ack.py` / `strip_sn_notice.py`). The original 2026-07-30 header's "1791 occurrences" figure was an uncorrected per-request-log count and never reproduced. **Emptied-block handling:** the marker occupies its own whole block among others in the measured real shape — content is emptied to `'.'` IN PLACE rather than spliced out of the content list. Splicing would shift every subsequent block's index (the wake-up block would move from index 2 to index 1), corrupting `rule_ops._ops_from_content_change`'s index-keyed old/new comparison (it would diff the marker's OLD text against the wake-up's NEW text as if one block were edited, and see the wake-up block itself as a deletion) — emptying in place keeps block count and indices stable, and matches the `'.'` convention every other pass uses for a block reduced to nothing (the API rejects an empty text block). Covers all 4 content shapes (str, list/text, list/tool_result-str, list/tool_result-list). Returns `(new_content, removed_chunks)` for `stripped_interrupt_marker` mod attribution via the `IM` rule code (`strip_vocab.py`). **(2026-09, helper-extraction milestone) `_strip_interrupt_marker` (was 50 LOC) delegates to the shared `payload_helpers._walk_replace_marker_blocks` walker** — see `strip_bg_launch_ack.py`'s own entry for the fold rationale (byte-identical walk shape, verified before folding).
**Reads:** Message content (string or list of blocks).
**Writes:** Nothing — returns `(modified_content, list[str])`.
**Called by:** `src/proxy/message_passes_simple.py` (`_apply_interrupt_marker_strip`).
**Calls out:** `payload_helpers` (`_walk_replace_marker_blocks`)

---

### strip_sn_notice.py (67 LOC)

**Purpose:** Strip the bare 4-line `[SYSTEM NOTIFICATION - NOT USER INPUT]` paragraph CC injects ahead of `<task-notification>` tags in background-task wake-up messages, from top-level `str` / `text`-block content only. **Anchored decision** — `_is_sn_notice(text)` returns True only when `text.lstrip().startswith(_SN_NOTICE_PARAGRAPH)`, same FP-nuke-class fix as `strip_bg_launch_ack.py` (see `process-docs/message_strip_fp_nuke/`): tool_result data quoting the paragraph, or user-pasted transcripts containing it mid-content, are never stripped. Does NOT descend into `tool_result` at all (mirrors `strip_bg_completed.py`'s top-level-only traversal) — the paragraph never genuinely occurs there. Covers `str` and `list[text]` shapes at ANY block index (not hardcoded to 0 — measured: fires at index 1 in some cases). `_strip_sn_notice_from_text` tries removing paragraph+`'\n\n'` first (standard form ahead of `<task-notification>`), falls back to paragraph-only (edge case: paragraph is the entire message). Distinct from the `'SN'` rule in `strip_sr.py`/`strip_vocab.py`, which strips the SAME paragraph text when it arrives `<system-reminder>`-WRAPPED (a structurally different occurrence, out of scope here) — `strip_vocab.py`'s `attribute_chunk` disambiguates the two via a `startswith` special-case (mirroring the existing `<task-notification>` special-case) since both chunks share `'[SYSTEM NOTIFICATION'` as a substring. Returns `(new_content, removed_chunks)` for `stripped_sn_notice_paragraph` mod attribution via the `SNP` rule code.
**Reads:** Message content (string or list of blocks).
**Writes:** Nothing — returns `(modified_content, list[str])`.
**Called by:** `src/proxy/message_passes_simple.py` (`_apply_sn_notice_strip`); `src/proxy/message_passes_wakeup.py` (`_strip_sn_notice`, in `_unwrap_full_sr_wrapper`)
**Calls out:** stdlib only.

---

### content_strip.py (167 LOC)

**Purpose:** Strip or extract non-SR content from API message payloads — rejection tool_result blocks, SessionStart SR extraction, session-guidance section removal, gitStatus stripping from sys[3], full sys[3] text replacement with `"."`, `tools[*].description` stripping, and `tools[*].input_schema.properties[*].description` (per-parameter) stripping. Both `_strip_sys3` and `_strip_tool_descriptions` capture pre-strip originals and return them as a third tuple element for log entry storage.
**Reads:** Message content (string or list of blocks); full payload dict for tool and system strip functions.
**Writes:** Nothing — returns modified content, extracted text, or modified payload.
**Called by:** `src/proxy/rules.py` (`_strip_session_guidance`, `_strip_git_status`); `src/proxy/message_passes.py` (`_message_has_rejection`, `_strip_rejection_message`); `src/proxy/addon.py` (`_strip_tool_descriptions`, `_strip_sys3`)
**Calls out:** —

---

### diff_engine.py (297 LOC)

**Purpose:** Align and classify spans from an Original↔Forwarded payload diff. Produces strip/inject/equal spans for system blocks (by index), tools (by name), messages (by index + inner block), and top-level scalar fields. Also provides the GT (ground-truth) span builder that replaces blind word-diff for message blocks when strip chunks are recorded. Single source of truth used by `logging.py` (runtime strip/inject delta writes) and `dev/proxy_dual_log/` (offline verification scripts). **(2026-09, helper-extraction milestone) `build_message_spans` (was 57 LOC) split into `_split_stripped_segments`** (step 1: split `orig_text` at stripped-chunk positions into `equal_segs`/`stripped_segs`+flags) **and `_build_fwd_spans`** (step 3: decompose `fwd_text` into injected/equal spans from `injected_chunks`) — `build_message_spans` itself is now the two calls plus the step-2 stripped-span emission loop.
**Reads:** Nothing — pure functions operating on payload dicts/lists passed as arguments.
**Writes:** Nothing — returns diff result lists/dicts.
**Called by:** `src/proxy/strip_inject_delta.py` (imports `_diff_system`, `_diff_tools`, `_diff_messages`, `_diff_top_level_fields`, `_get_inner_text`, `compose_block`); `dev/proxy_dual_log/diff_strip_inject.py` (via `sys.path.insert` + `from src.proxy.diff_engine import ...`).
**Calls out:** stdlib only (`json`, `difflib.SequenceMatcher`).

**Key functions:** `_diff_text(orig, fwd) -> list[tuple]` — word-level diff when `SequenceMatcher.ratio() >= RATIO_THRESHOLD (0.1)`, whole-block 2-span replacement otherwise. Called only by `_diff_system` and `_diff_tools` — NOT for messages (message spans use `compose_block` exclusively since Stage 4). `_diff_system`, `_diff_tools` — collection-level alignment with `_diff_text`. `_diff_messages(orig_msgs, fwd_msgs) -> list` — produces block structure only (each block_diffs entry: `{bidx, o_text, f_text}`, no `spans` field since Stage 4); spans for messages computed in `logging.py` via `compose_block`. `_diff_top_level_fields(orig_payload, fwd_payload) -> list` — iterates all keys in orig ∪ fwd, skips `_COLLECTION_KEYS = {"system","tools","messages"}`, classifies each non-collection key as stripped/injected/replaced; captures model override (`claude-opus-4-7` → `claude-opus-4-8`) as a `replaced` field entry. `_get_inner_text(block) -> str` — inner content extractor: returns `block["text"]` for text blocks, `block["content"]` (or `\n`-joined inner text for list content) for tool_result blocks, `json.dumps(block)` for other dicts. `apply_edit_to_spans(spans, offset, removed, injected) -> list` — applies one `(offset_in_Ck, removed, injected)` op to a span list; Ck = equal+injected cursor; equal bytes in removal range → stripped; injected bytes in removal range → disappear; new injected span inserted at splice point. Both invariants maintained: `equal+stripped==C0`, `equal+injected==Ck`. `compose_block(c0_text, block_ops) -> list` — composes all ops `[(offset, removed, injected)]` for one block into a span list over C0; initialises `[("equal", c0_text)]`, applies `apply_edit_to_spans` for each op in order. Proven byte-exact (9509/9509 blocks, money-shot TN+BG = 1 injected wakeup). Sole span-building path for messages since Stage 4; replaces the former `build_message_spans` / `_diff_text` fallback path.
New private helpers (same module): `_split_stripped_segments`, `_build_fwd_spans` (2026-09).

---

### logging.py (263 LOC)

**Purpose:** Build structured JSONL entries for `forwarded_delta` and `tool_error` dual-log records; compute message diffs vs previous request. Shared normalization helpers (`_strip_cache_control`, `_normalize_msg_shape_for_hash`, `_delta_hash`) are imported by `strip_inject_delta.py` for the stripped/injected pipeline. **(2026-09, helper-extraction milestone)** `_compute_diff` (was 52 LOC) split into `_diff_modified_count` (the shared-prefix modified-count scan) + `_build_diff_summary` (the human-readable summary string); `_build_forwarded_delta` (was 63 LOC) split into `_compute_delta_hashes`, `_build_section_deltas` (is_first vs changed-hash-only delta dicts), `_forwarded_timestamp`; `_build_errors_entries` (was 52 LOC) split into `_build_tool_use_name_map` and `_build_error_entry` (one record dict).
**Reads:** Raw payload dicts, message lists, previous message summaries, previous delta hash state.
**Writes:** Nothing — returns structured entry dicts.
**Called by:** `src/proxy/addon.py` (imports `_build_forwarded_delta`, `_build_errors_entries`); `src/proxy/cache.py` (imports `_summarize_message`, `_compute_diff`); `src/proxy/strip_inject_delta.py` (imports `_strip_cache_control`, `_normalize_msg_shape_for_hash`, `_delta_hash`)
**Calls out:** `src/proxy/message_summary.py` (`_summarize_message`)

**Log record types:** `_build_forwarded_delta(payload, request_id, prev_hashes) -> (entry, curr_hashes)` → `{type: "forwarded_delta", is_first, counts, system_delta, tools_delta, messages_delta, model, max_tokens, output_config, context_management, diagnostics}`; REQ#1 full, subsequent only changed elements; `max_tokens` and `output_config` always-include per entry (like `model`) so read-side gets fresh values without a separate scalar accumulator. **Shared hashing helpers** (imported by `strip_inject_delta.py`): `_strip_cache_control(obj)` — recursively removes `cache_control` keys for stable comparison; `_normalize_msg_shape_for_hash(msg)` — collapses single-text-block user-message list to plain string (mirrors `cache._normalize_user_content_shape`, cannot import from there due to circular dep); `_delta_hash(element)` — MD5[:10] of element after both normalizations, used for forwarded-delta hash chains and field-level hashes in the stripped/injected chain. `_build_errors_entries(payload, request_id, timestamp, seen_ids, worker_context, session_id, proxy_file) -> list` → scans `payload.messages` for `is_error=True` tool_result blocks not in `seen_ids`; builds `tu_name_map` from all `tool_use` blocks (id→name) to resolve `tool_name`; returns list of `{type: "tool_error", request_id, timestamp, ts, session_id, worker, tool_name, tool_use_id, error_full, proxy_file}` records. `_extract_tool_result_text(content) -> str` — handles both tool_result content shapes: plain string returned as-is; list-of-blocks joins all `text` block values with `\n`.
New private helpers (same module): `_diff_modified_count`, `_build_diff_summary`, `_compute_delta_hashes`, `_build_section_deltas`, `_forwarded_timestamp`, `_build_tool_use_name_map`, `_build_error_entry` (2026-09).

---

### strip_inject_delta.py (322 LOC)

**Purpose:** Build `stripped_delta` and `injected_delta` JSONL entries from an original↔forwarded payload pair. Computes per-section diffs via four pure-return helpers (`_process_system_section`, `_process_tools_section`, `_process_messages_section`, `_process_fields_section`), builds flat `loc_key → MD5[:10]` hash chains for delta suppression, and attributes each change to the responsible strip/inject function via `fn_map`. Fn-attribution constants (`_SYS_FN`, `_FIELD_STRIP_FN`, `_FIELD_INJECT_FN`, `_MSG_CODE_TO_FN`) and span-hash helpers (`_hash_spans`, `_hash_span_sequence`) live here. `_process_messages_section` uses role-based attribution for `role='system'` message strips: `role='system'` → `code = 'RS'` (bypasses `_attribute_chunk`) UNLESS the original content top-level-contains `<task-notification>` (`_top_level_content_contains`, imported from `payload_helpers`) — that subset is left untouched by `_apply_role_system_strip` (2026-07-29 TN fix, `message_passes.py`) and actually stripped by `_apply_first_pass`/`_apply_sn_notice_strip`, so attribution falls through to the normal `_attribute_chunk` content-based path instead, keeping `fn_map` accurate for whichever function really touched the content. **(2026-09, helper-extraction milestone)** `_process_messages_section` (was 63 LOC) split into `_block_c0_text` (before-side text for one block-diff), `_record_stripped_msg_block`/`_record_injected_msg_block` (one side's hash+fn-attribution recording, returning the value to store or `None`); `_build_stripped_injected_deltas` (was 73 LOC) split into `_compute_all_diffs` (normalize both payloads + all 4 section diffs + counts) and `_build_delta_entry` (one stripped/injected entry dict, called twice).
**Reads:** Original and forwarded payload dicts (cache_control stripped at call site); previous hash state dicts (`loc_key → MD5[:10]`) from prior request; `all_ops` (`{msg_idx: {blk_idx: [(offset, removed, injected)]}}`) bridged from `flow.metadata`.
**Writes:** Nothing — returns `(stripped_entry, injected_entry, new_stripped_hashes, new_injected_hashes)` 4-tuple.
**Called by:** `src/proxy/addon.py` (imports `_build_stripped_injected_deltas`, called in `response()` hook via metadata bridge)
**Calls out:** `src/proxy/diff_engine.py` (`_diff_system`, `_diff_tools`, `_diff_messages`, `_diff_top_level_fields`, `_get_inner_text`, `compose_block`); `src/proxy/strip_vocab.py` (`attribute_chunk` — fn_map attribution via `_MSG_CODE_TO_FN`); `src/proxy/logging.py` (`_strip_cache_control`, `_normalize_msg_shape_for_hash`, `_delta_hash`); `src/proxy/payload_helpers.py` (`_top_level_content_contains` — role='system' TN-exception check)

**`_build_stripped_injected_deltas(orig_payload, fwd_payload, request_id, prev_stripped, prev_injected, model, all_ops=None) -> (stripped_entry, injected_entry, new_s_hashes, new_i_hashes)`** — complete-payload diff; both payloads pre-normalized at call site; messages additionally normalized via `_normalize_msg_shape_for_hash` before diff. **Message span path (compose_block):** per block: `block_ops = msg_ops.get(bidx_int, [])` (empty default); `c0_text = _get_inner_text(orig_block)` always extracted; `spans = compose_block(c0_text, block_ops)`. Op-less (unmodified) blocks produce `[("equal", c0_text)]` → nothing logged. **_stripped format:** `system_delta[idx]`/`messages_delta[midx][bidx]`/`tools_delta[name]["desc"]` = flat stripped text lists; hash via `_hash_spans` (MD5[:10] of pipe-joined texts). **_injected format:** same locations store ordered `[(tag, text), ...]` span lists with tags `"equal"` / `"injected"`; written only when block has ≥1 injected span; hash via `_hash_span_sequence` (MD5[:10] of `tag:text|...`). **fn_map:** top-level `fn_map: {loc_key → fn_name}` in both entries; attribution: sys by index (`_SYS_FN = {1: '_apply_system_passes', 2: '_apply_system_passes', 3: '_strip_sys3'}` — entries 1 and 2 are explicit even though the `.get(idx, "_apply_system_passes")` fallback already resolves them the same way, kept explicit for readability/consistency with entry 3 which does differ from the fallback), tools by shape, messages via `attribute_chunk → _MSG_CODE_TO_FN`. **Exclusions:** `field.*` loc_keys not written to `fn_map` (overridden every request); message, system, and tools-desc blocks whose only injected text is `"."` are skipped (`"."` is the API-required empty-block placeholder from `_strip_sys3` / `_apply_system_passes` / `strip_sr.py`, not a real inject). The overlay dicts (`i_sys`, `i_tools`, `i_blks`) are always written when `has_i` — only fn attribution is suppressed. Old entries without `fn_map` are read-side safe.
New private helpers (same module): `_block_c0_text`, `_record_stripped_msg_block`, `_record_injected_msg_block`, `_compute_all_diffs`, `_build_delta_entry` (2026-09).

---

### message_summary.py (183 LOC)

**Purpose:** Summarize and classify message content for log entries — produces compact dicts with role, type, chars, preview, block counts, and cache_control presence. **(2026-09, helper-extraction milestone)** `_summarize_message` (was 64 LOC) split into `_summarize_block` (the whole per-block-type elif chain, building `chars`/`preview`/`full_text`/`has_cc` plus type-specific extra keys); `_classify_blocks` (was 55 LOC) split into `_extract_tool_result_parts` (the tool_result branch's own nested str/list handling and `"[tool_result]"` fallback) — the other 3 branches (text/tool_use/thinking) stayed inline, already small.
**Reads:** Raw message dicts from API payload.
**Writes:** Nothing — returns summary dicts.
**Called by:** `src/proxy/addon.py`, `src/proxy/logging.py`, `src/proxy/cache.py`
**Calls out:** —
New private helpers (same module): `_summarize_block`, `_extract_tool_result_parts` (2026-09).

---

### tool_injection.py (167 LOC)

**Purpose:** Deterministically append MCP tool schemas to `payload["tools"]` in stable order (always-injected plugin slot first, then active plugins in activation order), preventing cache rebuilds from alphabetical INSERT behavior. iterative-dev schemas removed — `_ALWAYS_INJECTED_PLUGIN` constant is retained but is a no-op until new schemas are added.
**Reads:** Schema store at `src/proxy/schemas/<plugin>/*.json` (one-time load); `<project>/.claude/active_plugins.json` (mtime-reloaded); `proxy_rules.json` exclude list.
**Writes:** Nothing — returns modified payload.
**Called by:** `src/proxy/addon.py`
**Calls out:** —

---

### tools.py (52 LOC)

**Purpose:** Two helpers used during the modification pipeline.
- `_strip_unused_tools(payload)` removes blocklisted tools from `payload["tools"]` using `TOOL_BLOCKLIST`. Returns `(modified_payload, count_removed, removed_names)` — the 3rd element preserves the dropped tool names for downstream stamping on the log entry as `stripped_unused_tools_names`.
- `_extract_deferred_tool_names(payload)` scans user-message content of the ORIGINAL payload (pre-modification) for the `<system-reminder>` block matching the deferred-tools identifier, parses the listed tool names, and returns a deduplicated list. Mirror of the linestart-anchored regex used in `strip_sr.py`. Stamped on the entry as `deferred_tools_names`. Reads from the original payload (not the modified copy) so it captures the SR before `apply_modification_rules` strips it.
**Reads:** Payload dict with tools list and (for deferred-extraction) `messages`.
**Writes:** Nothing — returns tuples / list per function.
**Called by:** `src/proxy/addon.py`
**Calls out:** stdlib only (`re`).

---

### fixation.py (73 LOC)

**Purpose:** Freeze `sys[2]` content and `msg[0]` project-rules block after the first request per model family, preventing byte-drift from rule-file reloads mid-session.
**Reads:** Modified payload dict; fixated state dict.
**Writes:** Nothing — returns updated fixated dict (capture) or modified payload (apply).
**Called by:** `src/proxy/addon.py`
**Calls out:** —

---

### inject_helpers.py (167 LOC)

**Purpose:** Two post-rules payload injections: model parameters (thinking/effort/max_tokens from `proxy_rules.json`) and the `context_management` block. `_inject_model_override(payload, model_family, fixated_model_override=None)` (2026-08-06 rework, fixation added 2026-09) is a two-path dispatcher on config key presence — `"model_params" in config` (presence, not truthiness; an empty `{}` still counts) routes to `_inject_model_params`: exact `payload["model"]` lookup against `config["model_params"]`, applies thinking/effort/max_tokens on a hit (each optional) and NEVER writes `model` — the session's model is chosen at start via `claude_proxy_start.sh`'s `--fable`/`--opus` flags (`src/claude_proxy_start.sh`), the proxy must not force it back. Config without a `model_params` key falls back unchanged to `_inject_legacy_model_override` — the pre-2026-08-06 family-bucketed behavior (`model_family=="opus"` → `model_override`, `"sonnet"` → `model_override_worker`, each gated by its own `enabled` flag), INCLUDING the `model`-field rewrite — byte-identical for an unmigrated config, the safe-rollout path. Legacy sections are ignored entirely once `model_params` is present, even if still in the file. **Fixation (2026-09, `process-docs/cache/cache_rebuild_cases.md` Case 3 precedent):** `fixated_model_override` is a caller-owned dict (production: `ProxyAddon.model_params_fixated`) keyed by EXACT model id. On the first call for a model id, resolves live exactly as before, then snapshots the WHOLE resolved unit (the per-model `entry` dict, or `{"model_override": {...}}`/`{"model_override_worker": {...}}` for the legacy path — never individual fields) into the dict under that key. Every subsequent call for that model id replays the pinned snapshot by calling `_inject_model_params`/`_inject_legacy_model_override` — themselves completely UNCHANGED — with a synthesized 1-entry config built from the snapshot (`_model_params_dict_for`), so `_load_config()` is never touched again for that model id and fresh vs. replay share the exact same apply code path (byte-identical by construction, not by separate implementations). A genuine miss (config loaded fine, no entry) IS pinned, same as a hit; a genuine `_load_config()` exception is NOT pinned (next call retries live). Omitting `fixated_model_override` (default `None`) creates a fresh, discarded dict per call, so every 2-arg call is independent — pre-fixation behavior, unaffected for any caller (incl. the pre-2026-09 half of `p2_model_params_probe.py`) that doesn't pass one.
**Reads:** Payload dict, model_family string, optional fixation dict; `proxy_rules.json` via `rules_config._load_config()` — only on a cache-miss for the request's exact model id (fixation dict empty or lacking that key).
**Writes:** Nothing on the payload — returns modified payload or `(modified_payload, injected_bool)`. Mutates the CALLER-OWNED `fixated_model_override` dict in place (one new key per newly-seen model id, never removed) — the only side effect in this module.
**Called by:** `src/proxy/addon.py` (`_run_post_fixation_pipeline`, passes `self.model_params_fixated`).
**Calls out:** —

---

### strip_vocab.py (280 LOC)

**Purpose:** Shared vocabulary + semantics for proxy strip classification. Single source of truth used by `dev/tool_use_analysis/strip_audit.py` and `src/proxy_display/` (monitor). MUST be updated in lockstep when `rules.py` adds/renames rules or changes markers. **(2026-09, helper-extraction milestone)** `classify_tags` (was 58 LOC) split into `_collect_delta_texts` (the blocks/content_preview/content_tail text-gathering loop) and `_tag_signal` (one tag literal's LEAK/SUS verdict — `classify_tags` itself now loops the 4 tag checks through it instead of 4 near-identical if-blocks); `classify_req` (was 71 LOC) split into `_compute_effective_chunks`, `_compute_inert_codes`, `_compute_idx_msgs` (one bucket computation each). Exports:
- Constants: `BUCKETS` (EFF/INERT/IDX/LEAK/SUS), `RULES` (CMD/SK/AT/DEF/NAG/TN/PYR/UI/PM/REJ/ALL/SC/IR/PP/BGK/BL/GL/BD/ENV/HP/SN/SNP/FM/RS/IM with markers), `TAG_LITERALS` (PO/SR/TN/ND), `STRIP_RULE_CODES`, `_SR_STRIP_RULES` (SR-class strip rule full names, used for LEAK:<SR> detection; excludes TN, SC, IR, PP, SNP).
- `attribute_chunk(chunk) -> code | None` — marker-substring attribution (starts-with special-case for TN and for `SNP` — the bare SN-notice paragraph shares `'[SYSTEM NOTIFICATION'` as a substring with the `<system-reminder>`-wrapped `SN` chunk, so `SNP` is special-cased ahead of the generic marker loop, exactly like `TN`, to avoid `SN` winning by dict-iteration order).
- `code_for_rule(full_name) -> code | None` — reverse lookup from `modifications[]` entry to rule code.
- `classify_tags(entry) -> (leak_signals, sus_signals)` — **delta-scoped**: scans `entry.messages[prev_message_count:][].blocks[].full_text` + content_preview/tail for the 4 tag literals. `prev_message_count = message_count - messages_added` (from `diff_from_prev`). Missing `diff_from_prev` → start=0 (first REQ, full scan). `first_diff_index < 0` (byte-identical re-fire sentinel) returns empty. Does NOT use `first_diff_index` as delta bound — `first_diff_index` can regress into old messages on 1-char re-serialization drift (TN strip appends `\n`) causing double-fire. Pairs each found tag with the relevant rule in `modifications[]` to decide LEAK vs SUS.
- `classify_req(entry, prev_entry) -> dict` — per-REQ 5-bucket classification. EFFECTIVE via chunk-tuple-diff against `prev.stripped_msg_removed` (skips chunks unchanged since prev). INERT via counter-delta on `modifications[]` filtered by "no chunks attributable". IDX from `smi` diff with empty `stripped_msg_removed[idx]`. LEAK/SUS delegate to `classify_tags` (inherits delta scope automatically — no separate first_diff_index handling needed).
- `legend_markdown() -> str` — Markdown legend block (3 tables: Buckets, Rules, Tag Literals) emitted at the top of audit reports.
**Reads:** Nothing at module level.
**Writes:** Nothing — pure data + helpers.
**Called by:** `dev/tool_use_analysis/strip_audit.py` (via sys.path insertion) for full legend + classify_req delegation; `src/proxy_display/render_messages.py` for attribute_chunk, classify_tags, code_for_rule, classify_req (monitor `_aggregate_req_buckets` is a thin delegate); `src/proxy/strip_inject_delta.py` (imports `attribute_chunk as _attribute_chunk` for fn_map attribution in `_build_stripped_injected_deltas`).
**Calls out:** `collections.Counter` (counter-delta inside classify_req).
New private helpers (same module): `_collect_delta_texts`, `_tag_signal`, `_compute_effective_chunks`, `_compute_inert_codes`, `_compute_idx_msgs` (2026-09).

---

### payload_helpers.py (245 LOC)

**Purpose:** Low-level payload content inspection and manipulation used by `rules.py` — find/strip system-reminder blocks, strip blocklisted tool_reference blocks, replace task-notification XML tags. Exports two content-search helpers with distinct scopes: `_content_contains` (descends into tool_result — still correct for the non-SR passes: git-lock, hook-prefix, bd-noise genuinely need to match tool_result content); `_top_level_content_contains` (top-level str/text only, never tool_result — used by wakeup-injection guards AND, since 2026-07-28, by every SR-family gate in `message_passes.py`/`message_passes_simple.py` — see Gotchas). `_find_system_reminder_blocks` and `_find_all_system_reminder_blocks` are top-level-only (str / list[type=='text']) as of the same 2026-07-28 fix — their `tool_result` branch was removed so `pass_removed_by_idx` bookkeeping matches what `strip_sr.py` actually removes; both functions are called exclusively by the SR-family passes in `message_passes.py` (grep-confirmed no other callers), so this was safe without touching any other pass. Patterns still include `\n?` after `</system-reminder>` (with `re.DOTALL`) — recorded chunk includes the trailing newline that `_STANDALONE_SR_RE` strips, closing the precision gap in GT span building. Task-notification helpers: `_extract_task_notification_output_file` — extracts the `<output-file>` path from the first TN block in content, returns `''` if absent; `_extract_task_notification_task_id` (2026-07-29) — extracts `<task-id>` the same way, same `''`-if-absent contract, mirrors the output-file extractor exactly (`<task-id>(.*?)</task-id>`, DOTALL, searched inside `_find_task_notification_blocks(content)`); `_replace_task_notification_tags(content, replacement_text)` — replaces TN blocks inline with `replacement_text` using lambda re.sub (no backslash-sequence interpretation), used by both completed and failed TN paths to produce a single block. **`_walk_replace_marker_blocks(content, predicate, replace_fn)` (2026-09, new, helper-extraction milestone)** — shared block walk for a "predicate matches whole text -> replace whole text" strip, covering all 4 content shapes; folds `strip_bg_launch_ack.py`'s and `strip_interrupt_marker.py`'s own walks, which were byte-identical in shape (only the predicate and replace_fn differed) — verified before folding. Checked but did NOT fold (different shape — no tool_result descent, or an always-transform-then-compare shape rather than predicate-then-replace): `strip_sn_notice.py`, `strip_hook_prefix.py`, `strip_git_lock.py`, `strip_bd_noise.py`, `strip_po.py`, `strip_bg_completed.py` — see `process-docs/proxy/` for the full per-module comparison.
**Reads:** Message content (string or list), payload dicts.
**Writes:** Nothing — returns modified content or filtered dicts.
**Called by:** `src/proxy/rules.py` (`_strip_blocked_tool_references` re-export); `src/proxy/message_passes.py` (`_find_system_reminder_blocks`, `_find_all_system_reminder_blocks`, `_find_task_notification_blocks`, `_content_contains`, `_top_level_content_contains`, `_extract_task_notification_output_file`, `_extract_task_notification_task_id`, `_replace_task_notification_tags`); `src/proxy/message_passes_simple.py` (`_content_contains`, `_top_level_content_contains`); `src/proxy/strip_bg_launch_ack.py`, `src/proxy/strip_interrupt_marker.py` (`_walk_replace_marker_blocks`)
**Calls out:** —

---

## State

`tool_injection.py` holds four module-level caches (set once per mitmproxy process):
- `_SCHEMA_STORE_CACHE` — all plugin schemas loaded from `src/proxy/schemas/`
- `_ACTIVE_PLUGINS_CACHE`, `_ACTIVE_PLUGINS_MTIME`, `_ACTIVE_PLUGINS_PATH` — active plugin list with mtime-based reload

`addon.py` owns `ProxyAddon` instance state, split (2026-09, addon-split milestone) across 4 collaborator objects defined in `addon_state.py` — see that module's entry for the full attribute list. `self.delta.messages_by_model` for BP3 unchanged-prefix detection; `self.delta.forwarded_hashes_by_model` for `_forwarded` delta chain; `self.delta.stripped_hashes_by_model` / `self.delta.injected_hashes_by_model` (flat `loc_key → MD5[:10]` dicts) for `_stripped`/`_injected` delta chains; `self.delta.error_ids_by_model` (sets of `tool_use_id` strings) for `_errors` dedup chain; `self.fixation.model_params_fixated` (2026-09, dict keyed by exact model id — see `inject_helpers.py`) pins the resolved `model_params`/legacy-override snapshot per model id for the process lifetime, mirroring `self.fixation.fixated`'s sys2/msg0 protection but a separate dict at a different key granularity. All state resets on mitmproxy hot-reload. `self.identity.session_id` and `self.identity.worker_context` are computed once at `__init__` and are immutable for the proxy's lifetime (in production — see `addon.py`'s State entry for the dev/ probes that overwrite `worker_context` post-construction).

`bg_escape.py` owns `_escaped_task_ids` — a **module-global** `set` (not per-`ProxyAddon`-instance) of background-task ids that have already fired an Escape, for the lifetime of this proxy process. Resets to empty on mitmproxy hot-reload (any edit under `src/proxy/`, same trigger as every other cache in this package) or a full process restart. Consequence: a restart can cause at most ONE extra Escape per task id whose ack text is still present in the worker's resent conversation history — never a repeat storm, since the freshly-restarted process re-adds the id to its own set on first sighting. `bg_escape_events.jsonl` is append-only across restarts (unlike `_escaped_task_ids`, which is memory-only) — a restart-caused extra fire is still visible in the log even though the in-memory dedup state that would have prevented it is gone.

## Gotchas

**An op-less strip still rewrites the payload — it only loses its LOG entry, and that failure is silent.** `message_passes` sets the new content directly (`{**msg, "content": "."}`) and records the op separately via `_ops_from_content_change`. The two are independent: if the op comes back empty, the forwarded payload is still correct and the API still receives the stripped version, but `strip_inject_delta._process_messages_section` skips the message entirely (`if s_texts:`), so nothing marks it in the dual log and no pane or CLI can ever show it. That is exactly how the list→str shape gap went unnoticed for so long — nothing downstream fails loudly, the evidence simply never gets written. When adding a pass that changes content shape, verify the op is produced, not just that the payload is right: replay a recorded `_original` through `rules.apply_modification_rules` and assert the message index appears in `all_ops`.

**A second Escape into an idle CC TUI opens the quit menu — `bg_escape.py`'s dedup is load-bearing, not an optimization.** `tmux send-keys ... Escape` is a no-op at an empty prompt but a SECOND one (menu already open, or any other non-empty-prompt state) can open Claude Code's quit menu. Since the raw launch-ack text stays in a worker's own conversation history and is resent on nearly every subsequent request (measured 142/169), `_trigger_bg_escape`'s per-task-id `_escaped_task_ids` set is the only thing preventing a re-fire on every one of those requests. Any change to this pass must preserve "at most one Escape per task id, ever" — never relax it to "per request" or "per tick".

**Hot-reload causes cache rebuilds.** mitmproxy hot-reloads addon scripts on any file change on disk, resetting `ProxyAddon.prev_messages_by_model` → BP3 can no longer find the unchanged prefix → full cache rebuild. `claude_proxy_start.sh` works around this by copying both `proxy_addon.py` AND the entire `src/proxy/` package to `src/logs/.proxy_live_<id>/proxy/` at startup. Never edit files in `src/proxy/` during a live session expecting the running proxy to stay isolated — only git merges are blocked by the freeze; direct edits to the live copy affect it immediately.

**Post-merge load test is mandatory.** After ANY merge touching `src/proxy/` or `src/proxy_addon.py`, run:
```bash
cd src/logs && mitmdump -s ../.proxy_addon_live_*.py --set flow_detail=0 -q -p 0 2>&1 &
PID=$!; sleep 3; kill $PID 2>/dev/null; wait $PID 2>/dev/null
```
mitmproxy import errors are silent — the proxy crashes on startup and workers get ECONNREFUSED.

**Worker proxies are frozen at spawn time.** Each worker's proxy package snapshot in `src/logs/.proxy_live_worker_<name>/` never updates. A worker spawned before a proxy-touching merge cannot reach new behavior or new imports. Before `worker_send` for a proxy task: check spawn time, run `git log --since='<spawn-time>' -- src/proxy/`, and kill+respawn if any merges are found.

**SR stripping is template-based, not regex-greedy.** `strip_sr.py` maintains a catalog of known SR templates (task-tools-nag, pyright-new-diagnostics, deferred-tools, user-interrupt, system-notification, file-modified, claudemd-contents, date-changed, skills-available, plan-mode). Each template has one or more identifier-strings; matching uses `startswith` against the extracted SR-block inner text, not a greedy regex. This is the fix for the historical false-positive bug where `<system-reminder>.*?</system-reminder>` matched across code literals (e.g. `if "<system-reminder>" in text:` in a tool_result) and stripped real user code. Adding a new strip rule = add a new template entry with its identifier-string (or list of identifiers for OR semantics).

**Tool_result.content is NEVER strippable by the SR family (2026-07-28 — supersedes the prior "CC does inject SRs there" note, which was itself the FP-nuke bug).** `strip_sr.py::_strip_system_reminders` returns every `tool_result` block by identity; `_apply_first_pass`'s SR branches and `_apply_cumulative_sr_strips` gate via `_top_level_content_contains` (not `_content_contains`); `_find_system_reminder_blocks`/`_find_all_system_reminder_blocks` (`payload_helpers.py`) are top-level-only to match. Measured over ~660 real requests: 0 genuine SR-family injections inside tool_result, 1 confirmed FP (a RAG-retrieved doc excerpt quoting the env-context SR literally). The non-SR passes (git-lock, hook-prefix, bd-noise, po-preview, bg-launch-ack) are NOT affected — their own tool_result descent is correct and untouched; see `process-docs/message_strip_fp_nuke/2026-07-28_tool_result_sr_audit.md`.

**Strip-tracking is guarded per-rule.** In `message_passes.py` `_apply_cumulative_sr_strips`, each rule (skills / claudemd / pyright) appends to `pass_mods` only if its strip function actually changed the content (`new_content != content`). Without this guard, `_top_level_content_contains` could match a marker that `_strip_system_reminder` then fails to strip (e.g. template identifier mismatch) — and `pass_mods` would incorrectly mark the rule as fired, polluting `modifications` and causing `stripped_msg_removed` to capture chunks that still survive in `raw_payload`. Root case: claudemd `"Contents of "` identifier vs CC's preamble form `"As you answer the user's questions"` — two distinct SR variants for the same template that previously went unmatched.

**Pyright-strip lives in the second pass, not the first-pass elif-chain.** `message_passes.py` has two passes: `_apply_first_pass` — an `elif`-chain (exclusive per message, a message that hits one branch cannot trigger another) — and `_apply_cumulative_sr_strips` — a cumulative pass applied to every user message. Pyright diagnostics SRs can co-occur in the same message with Skills or claudeMd SRs; if pyright lived in the elif-chain it would be silently skipped for those messages. Any new rule that can co-occur with an existing first-pass rule MUST go to the second pass. The structural separation between `_apply_first_pass` and `_apply_cumulative_sr_strips` in `message_passes.py` enforces this invariant.


**`_PRESERVE_PREAMBLE` guard in strip_sr.py.** `strip_sr.py` has a hard-coded guard that prevents stripping claudeMd-context SR blocks: an SR whose inner text starts with `"As you answer the user's questions, you can use the following context:"` is always preserved verbatim, regardless of template matching. This allows the CLAUDE.md project-context block (injected by CC as a claudeMd SR with preamble) to survive the claudemd-strip rule. Adding new "preserve entire block" logic: mirror this pattern — `startswith` check in the extractor before template dispatch. **Exception: any new strip rule whose target block shares the same preamble (e.g. env-context SR) MUST insert its check BEFORE the `_PRESERVE_PREAMBLE` guard** — otherwise the guard fires first and the strip never runs. See `_ENV_CONTEXT_RE.fullmatch(inner)` check in `_apply_sr_strip._replace` for the pattern.

**`_apply_role_system_strip`'s nuke has 2 carve-outs, not 1 — both content-anchored, checked BEFORE the unconditional `.`-nuke.** Truncation-notice preserve (`startswith("[Truncated:")`) and TN preserve (`_top_level_content_contains(..., "<task-notification>")`, 2026-07-29) — CC delivers bg-task wake-ups as a plain-str `role='system'` message on some paths (measured: 173/280 real TN occurrences in one session were role='system', vs 107 role='user'). The TN carve-out does NOT build the wake-up itself — it leaves the message untouched for `_apply_sn_notice_strip` + `_apply_first_pass`'s TN branch (both widened to accept `role='system'`, narrowly gated on the TN tag) to do that, avoiding a second copy of the wake-up-construction logic. Any FUTURE carve-out here must follow the same shape: anchored content check, no reconstruction logic duplicated from elsewhere. `strip_inject_delta.py::_process_messages_section`'s `role=='system' → code='RS'` attribution shortcut mirrors the TN carve-out exactly (falls through to `_attribute_chunk` for TN-carrying role='system' messages) — a new carve-out here needs the matching attribution exception too, or `fn_map` will mislabel it as the blanket nuke.
